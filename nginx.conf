server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: unknown paths (client-side routes) serve index.html, not 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
