VITE_API_URL=http://localhost:3000
VITE_WS_URL=ws://localhost:3000
# Must be the @username of the bot that BOT_TOKEN (backend) belongs to — the Telegram Login
# Widget on /login needs it to know which bot to authenticate against.
VITE_TELEGRAM_BOT_USERNAME=
